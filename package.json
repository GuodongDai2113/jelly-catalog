{
  "name": "jelly-catalog",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "format": "prettier --write \"**/*.{js,css,json,md}\"",
    "format:check": "prettier --check \"**/*.{js,css,json,md}\""
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
